{
  "name": "mustard",
  "version": "1.0.0",
  "private": true,
  "description": "Personal knowledge store — monorepo for MCP server, TUI, and data layer",
  "workspaces": [
    "core",
    "cli",
    "mcp",
    "tui",
    "relay"
  ],
  "scripts": {
    "test": "cd core && npm test && cd ../mcp && npm test && cd ../relay && npm test && cd .. && node tui/tests/db.test.js",
    "test:core": "cd core && npm test",
    "test:mcp": "cd mcp && npm test",
    "test:relay": "cd relay && npm test",
    "test:tui": "node tui/tests/db.test.js",
    "build": "cd core && npm run build && cd ../cli && npm run build && cd ../mcp && npm run build && cd ../relay && npm run build"
  }
}
