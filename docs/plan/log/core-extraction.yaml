# Phase log: core-extraction
# Started: 2026-04-03T01:54:58Z

entries:
  - task: 0
    story: phase
    type: setup
    description: "Phase start — archived monorepo-foundation, ran reconciliation gate (R0-R5), created build/core-extraction branch, wrote phase-goal.md with 4 stories (35 criteria), set verify to npm test."
    result: pass
    timestamp: "2026-04-03T01:54:58Z"
    tokens: ~8000
  - task: 1
    story: US-C1
    type: investigate
    description: "Read mcp/src/db.ts and all tools (crud.ts, search.ts, links.ts, context.ts, summary.ts). Cataloged: db.ts exports getDb (singleton), initSchema (4 migrations + DDL), checkFtsHealth, rebuildFts, closeDb. All MCP tools return formatted strings — core must return typed data objects. RecordRow defined 3x — core unifies. VALID_LOG_TYPES + DEFAULT_STATUS in crud.ts → move to core. projectSummary lives in context.ts not summary.ts."
    result: pass
    timestamp: "2026-04-03T01:58:00Z"
    tokens: ~12000
  - task: 2
    story: US-C1
    type: implement
    description: "Created core/ package scaffold — package.json (mustard-core, ESM, better-sqlite3), tsconfig.json (ES2022+), src/db.ts (extracted from mcp/src/db.ts with identical schema/migrations/FTS), src/index.ts barrel. Configured npm workspaces in root package.json. Build and tests pass."
    result: pass
    notes: "✓ US-C1 complete"
    timestamp: "2026-04-03T02:02:00Z"
    tokens: ~8000
  - task: 3
    story: US-C2
    type: investigate
    description: "Investigation already done in task 1 — MCP tools return strings, core must return typed objects. RecordRow defined 3x needs unification. VALID_LOG_TYPES + DEFAULT_STATUS move to core. Skipping redundant investigate, proceeding to implement types.ts + records.ts."
    result: pass
    timestamp: "2026-04-03T02:03:00Z"
    tokens: ~1000
